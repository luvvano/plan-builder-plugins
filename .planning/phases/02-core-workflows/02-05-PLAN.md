---
phase: 02-core-workflows
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - openclaw-plugin/skills/workflows/verify-work/SKILL.md
  - openclaw-plugin/skills/workflows/verify-work/stage-verify.md
autonomous: true
requirements:
  - CMD-03
  - CMD-04
  - AGNT-01
  - AGNT-02

must_haves:
  truths:
    - "User can run /gsd:verify-work <N> and it presents testable behaviors extracted from plan SUMMARYs"
    - "The workflow presents tests as an inline checklist without using AskUserQuestion"
    - "The gsd-verifier agent role is faithfully embedded in stage-verify.md"
    - "If tests fail, the verifier produces a gap analysis identifying unmet requirements"
    - "The workflow outputs VERIFICATION.md in the phase directory summarizing pass/fail results"
  artifacts:
    - path: "openclaw-plugin/skills/workflows/verify-work/SKILL.md"
      provides: "Orchestrator for /gsd:verify-work — user-invocable, auto-mode UAT flow"
      contains: "name: gsd:verify-work"
    - path: "openclaw-plugin/skills/workflows/verify-work/stage-verify.md"
      provides: "Verification stage embedding gsd-verifier role for gap analysis"
      contains: "user-invocable: false"
  key_links:
    - from: "openclaw-plugin/skills/workflows/verify-work/SKILL.md"
      to: "openclaw-plugin/skills/workflows/verify-work/stage-verify.md"
      via: "@-path reference when gap analysis needed"
      pattern: "@.*stage-verify\\.md"
    - from: "openclaw-plugin/skills/workflows/verify-work/SKILL.md"
      to: "gsd-tools.cjs"
      via: "init verify-work subcommand for phase context"
      pattern: "GSD_TOOLS_PATH.*init verify-work"
---

<objective>
Create the full /gsd:verify-work workflow with auto-mode UAT testing and gap analysis capability.

Purpose: This is the verification command that validates whether a phase's success criteria are met. It extracts testable behaviors from plan SUMMARYs, presents them as an inline checklist, and runs gap analysis if tests fail. The auto-mode adaptation presents tests in chat without requiring AskUserQuestion.
Output: 2 SKILL.md files — orchestrator (user-invocable, auto-mode UAT), stage-verify.md (verifier agent for gap analysis).
</objective>

<execution_context>
@/Users/egorproskyrin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorproskyrin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-workflows/02-CONTEXT.md
@.planning/phases/02-core-workflows/02-RESEARCH.md
@.planning/phases/02-core-workflows/02-01-SUMMARY.md

<!-- Source agent file to port verbatim -->
@/Users/egorproskyrin/.claude/agents/gsd-verifier.md

<!-- Source workflow to port -->
@/Users/egorproskyrin/.claude/get-shit-done/workflows/verify-work.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:verify-work orchestrator SKILL.md with auto-mode UAT</name>
  <files>openclaw-plugin/skills/workflows/verify-work/SKILL.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/verify-work/SKILL.md` — the user-invocable orchestrator with auto-mode UAT.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd:verify-work
    description: Verify phase completion by testing success criteria against actual implementation. Presents testable behaviors and runs gap analysis.
    user-invocable: true
    os: ["darwin", "linux"]
    ---
    ```

    **Body structure — step-by-step recipe:**

    ```markdown
    # /gsd:verify-work

    Verify phase completion against success criteria.

    ## Arguments

    The user provides the phase number: `/gsd:verify-work 2`.
    If no argument provided, ask which phase to verify.

    ## Step 1: Initialize

    ```bash
    INIT=$(node "$GSD_TOOLS_PATH" init verify-work "$PHASE_ARG")
    ```
    Parse JSON: checker_model, commit_docs, phase_found, phase_dir, phase_number, phase_name, has_verification.

    If `phase_found` is false: inform user and stop.

    ## Step 2: Gather Evidence

    Read all SUMMARY.md files from the phase directory:
    ```bash
    ls $PHASE_DIR/*-SUMMARY.md 2>/dev/null
    ```

    Read the ROADMAP.md success criteria for this phase.
    Read any must_haves from the PLAN.md frontmatter files.

    If no SUMMARY files exist: inform user that execution has not completed, suggest `/gsd:execute-phase` first.

    ## Step 3: Extract Testable Behaviors

    From SUMMARY files and ROADMAP success criteria, extract a list of testable behaviors. Each behavior should be:
    - Specific and verifiable
    - Tied to a success criterion or must_have truth
    - Testable via code inspection, running commands, or checking file existence

    ## Step 4: Present Test Checklist

    Present the tests as an inline checklist in the chat:

    ```
    ## Phase <N> Verification Tests

    I'll now verify each success criterion:

    ### Test 1: [Description]
    **Criterion:** [From ROADMAP success criteria]
    **How to verify:** [Command or inspection]
    **Result:** [Run the verification and report PASS/FAIL]

    ### Test 2: [Description]
    ...
    ```

    For each test:
    1. State what is being tested
    2. Run the verification command or inspect the code
    3. Report PASS or FAIL with evidence
    4. If FAIL, note what is missing or broken

    **Auto-mode behavior:** Do NOT use AskUserQuestion. Run all verifications yourself using available tools (Bash, Read, Grep). Report results directly. If a verification requires manual user action (e.g., opening a browser), note it as MANUAL and describe what the user should check.

    ## Step 5: Gap Analysis (if failures)

    If any tests FAIL:

    Follow the gap analysis stage:
    @./stage-verify.md

    The verifier analyzes failures and produces a structured gap report.

    ## Step 6: Write VERIFICATION.md

    Write `$PHASE_DIR/VERIFICATION.md` with:
    - Test results (pass/fail for each criterion)
    - Overall result: PASSED or FAILED
    - Gap analysis (if applicable)
    - Recommended next steps

    If `commit_docs` is true:
    ```bash
    node "$GSD_TOOLS_PATH" commit "docs($PHASE): add verification results" --files "$PHASE_DIR/VERIFICATION.md"
    ```

    ## Step 7: Report

    Report to user:
    - Overall: PASSED or FAILED (X/Y tests passed)
    - If PASSED: Phase complete, suggest updating roadmap
    - If FAILED: Gap analysis summary, suggest `/gsd:plan-phase --gaps $PHASE` for gap closure
    ```
  </action>
  <verify>
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/verify-work/SKILL.md` shows `name: gsd:verify-work`
    Verify no AskUserQuestion: `grep -i "AskUserQuestion" openclaw-plugin/skills/workflows/verify-work/SKILL.md | wc -l` (should be 0, or only in "do NOT use" context)
    Verify auto-mode: `grep -i "auto-mode\|auto mode" openclaw-plugin/skills/workflows/verify-work/SKILL.md`
    Verify stage reference: `grep "@.*stage-verify" openclaw-plugin/skills/workflows/verify-work/SKILL.md`
    Verify VERIFICATION.md output: `grep "VERIFICATION.md" openclaw-plugin/skills/workflows/verify-work/SKILL.md`
  </verify>
  <done>Orchestrator SKILL.md created with 7-step auto-mode UAT flow. Runs all verifications autonomously, presents results inline, triggers gap analysis on failure, and writes VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create stage-verify.md with gsd-verifier agent role</name>
  <files>openclaw-plugin/skills/workflows/verify-work/stage-verify.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/verify-work/stage-verify.md` — embeds the gsd-verifier agent role verbatim for gap analysis.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd-verify-work-stage-verify
    description: Gap analysis stage for /gsd:verify-work — internal use only
    user-invocable: false
    os: ["darwin", "linux"]
    ---
    ```

    **Body:**
    1. Embed the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-verifier.md` verbatim:
       ```markdown
       <!-- AGENT ROLE: gsd-verifier -->
       <!-- Source: gsd-verifier.md (verbatim copy — DO NOT modify) -->

       [FULL VERBATIM COPY of gsd-verifier.md content here]
       ```

    2. Add gap analysis instructions:
       ```markdown
       ## Gap Analysis Instructions

       This stage is called when verification tests have FAILED. The orchestrator passes:
       - List of failed tests with evidence
       - Phase success criteria from ROADMAP.md
       - SUMMARY.md contents from completed plans

       Follow the gsd-verifier role above to:
       1. Analyze each failure — determine root cause (missing code, wrong implementation, incomplete wiring)
       2. Categorize gaps by severity (critical, major, minor)
       3. Map gaps to specific requirements that are unmet
       4. Produce a structured gap report

       Return the gap analysis as structured output that `/gsd:plan-phase --gaps` can consume to create gap closure plans.

       Format:
       ```json
       {
         "status": "FAILED",
         "passed": N,
         "failed": M,
         "gaps": [
           {
             "requirement": "REQ-ID",
             "description": "What is missing or broken",
             "severity": "critical|major|minor",
             "evidence": "What was checked and what failed",
             "suggested_fix": "High-level fix direction"
           }
         ]
       }
       ```
       ```

    **CRITICAL:** The gsd-verifier agent role must be a COMPLETE, VERBATIM copy.
  </action>
  <verify>
    Verify verifier role: `grep "gsd-verifier" openclaw-plugin/skills/workflows/verify-work/stage-verify.md`
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/verify-work/stage-verify.md` shows `user-invocable: false`
    Verify no user-local paths: `grep "@/Users/" openclaw-plugin/skills/workflows/verify-work/stage-verify.md | wc -l` (should be 0)
    Verify gap analysis output format: `grep "gaps\|severity\|suggested_fix" openclaw-plugin/skills/workflows/verify-work/stage-verify.md`
  </verify>
  <done>stage-verify.md created with gsd-verifier agent role embedded verbatim. Gap analysis instructions produce structured JSON output consumable by /gsd:plan-phase --gaps for gap closure planning.</done>
</task>

</tasks>

<verification>
1. `ls openclaw-plugin/skills/workflows/verify-work/` lists SKILL.md, stage-verify.md
2. `grep "name: gsd:verify-work" openclaw-plugin/skills/workflows/verify-work/SKILL.md`
3. `grep "user-invocable: true" openclaw-plugin/skills/workflows/verify-work/SKILL.md`
4. `grep "user-invocable: false" openclaw-plugin/skills/workflows/verify-work/stage-verify.md`
5. `grep -r "@/Users/" openclaw-plugin/skills/workflows/verify-work/` returns NO results
6. No AskUserQuestion in any file (or only in "do NOT use" instructional context)
7. VERIFICATION.md output path documented
8. Gap analysis JSON format documented for /gsd:plan-phase --gaps consumption
</verification>

<success_criteria>
- /gsd:verify-work orchestrator runs fully autonomous UAT without AskUserQuestion
- All verifications executed by the LLM using available tools (Bash, Read, Grep)
- VERIFICATION.md written to phase directory with pass/fail results
- Gap analysis triggered on failure using embedded gsd-verifier role
- Gap report structured as JSON for downstream consumption by /gsd:plan-phase --gaps
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-workflows/02-05-SUMMARY.md` per the summary template.
</output>
