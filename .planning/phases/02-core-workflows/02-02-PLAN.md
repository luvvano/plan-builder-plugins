---
phase: 02-core-workflows
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - openclaw-plugin/skills/workflows/new-project/SKILL.md
  - openclaw-plugin/skills/workflows/new-project/stage-setup.md
  - openclaw-plugin/skills/workflows/new-project/stage-roadmap.md
autonomous: true
requirements:
  - CMD-03
  - CMD-04
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - ORCH-01

must_haves:
  truths:
    - "User can run /gsd:new-project and it initializes a complete GSD project without any AskUserQuestion prompts"
    - "The workflow produces PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, and config.json in the .planning/ directory"
    - "The gsd-project-researcher agent role is faithfully embedded in stage-setup.md with all original instructions preserved"
    - "The gsd-research-synthesizer agent role is faithfully embedded in stage-setup.md with all original instructions preserved"
    - "The gsd-roadmapper agent role is faithfully embedded in stage-roadmap.md with all original instructions preserved"
    - "The orchestrator SKILL.md references stage files via @-path at the point of use, not all upfront"
  artifacts:
    - path: "openclaw-plugin/skills/workflows/new-project/SKILL.md"
      provides: "Orchestrator for /gsd:new-project — user-invocable, step-by-step recipe"
      contains: "name: gsd:new-project"
    - path: "openclaw-plugin/skills/workflows/new-project/stage-setup.md"
      provides: "Setup stage embedding gsd-project-researcher (4 research domains) and gsd-research-synthesizer roles"
      contains: "user-invocable: false"
    - path: "openclaw-plugin/skills/workflows/new-project/stage-roadmap.md"
      provides: "Roadmap stage embedding gsd-roadmapper role"
      contains: "user-invocable: false"
  key_links:
    - from: "openclaw-plugin/skills/workflows/new-project/SKILL.md"
      to: "openclaw-plugin/skills/workflows/new-project/stage-setup.md"
      via: "@-path reference at Step 3 of orchestrator"
      pattern: "@.*stage-setup\\.md"
    - from: "openclaw-plugin/skills/workflows/new-project/SKILL.md"
      to: "openclaw-plugin/skills/workflows/new-project/stage-roadmap.md"
      via: "@-path reference at Step 4 of orchestrator"
      pattern: "@.*stage-roadmap\\.md"
    - from: "openclaw-plugin/skills/workflows/new-project/SKILL.md"
      to: "gsd-tools.cjs"
      via: "node $GSD_TOOLS_PATH init new-project shell command"
      pattern: "GSD_TOOLS_PATH.*init new-project"
---

<objective>
Create the full /gsd:new-project workflow as an orchestrator SKILL.md with 2 stage skills embedding all required agent roles inline.

Purpose: This is the entry point for all new GSD projects. It replaces the Phase 1 proof-of-concept with a full workflow that runs 4 research domains, synthesizes findings, and produces a complete roadmap — all autonomously.
Output: 3 SKILL.md files — orchestrator (user-invocable), stage-setup.md (researchers + synthesizer), stage-roadmap.md (roadmapper).
</objective>

<execution_context>
@/Users/egorproskyrin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorproskyrin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-workflows/02-CONTEXT.md
@.planning/phases/02-core-workflows/02-RESEARCH.md
@.planning/phases/02-core-workflows/02-01-SUMMARY.md

<!-- Source agent files to port verbatim into stage skills -->
@/Users/egorproskyrin/.claude/agents/gsd-project-researcher.md
@/Users/egorproskyrin/.claude/agents/gsd-research-synthesizer.md
@/Users/egorproskyrin/.claude/agents/gsd-roadmapper.md

<!-- Source workflow to port -->
@/Users/egorproskyrin/.claude/get-shit-done/workflows/new-project.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:new-project orchestrator SKILL.md</name>
  <files>openclaw-plugin/skills/workflows/new-project/SKILL.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/new-project/SKILL.md` — the user-invocable orchestrator that drives the full new-project workflow.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd:new-project
    description: Initialize a new GSD spec-driven development project. Researches the domain, synthesizes findings, and produces a complete roadmap.
    user-invocable: true
    os: ["darwin", "linux"]
    ---
    ```

    **Body structure — step-by-step recipe the LLM follows:**

    ```markdown
    # /gsd:new-project

    Initialize a new GSD spec-driven development project.

    ## Step 1: Initialize

    Run init to get project context:
    ```bash
    INIT=$(node "$GSD_TOOLS_PATH" init new-project)
    ```
    Parse JSON output for: researcher_model, synthesizer_model, roadmapper_model, commit_docs, project_exists, has_codebase_map, planning_exists, has_existing_code, has_package_file, is_brownfield, needs_codebase_map, has_git, project_path.

    If `project_exists` is true: inform the user and stop. Do NOT overwrite an existing project.

    ## Step 2: Gather Project Idea

    Read the user's input from the current conversation. The user should have provided a project description, idea document, or pasted text explaining what they want to build.

    If no idea is provided in the conversation, ask the user to describe their project. Do NOT use AskUserQuestion — simply state what you need in the chat and wait for the user's next message.

    ## Step 3: Research & Synthesize

    Follow the research and synthesis stage:
    @./stage-setup.md

    Pass the project idea text and init context to the stage.

    ## Step 4: Build Roadmap

    Follow the roadmap stage:
    @./stage-roadmap.md

    Pass the synthesized research output and project context to the stage.

    ## Step 5: Commit

    If `commit_docs` is true:
    ```bash
    node "$GSD_TOOLS_PATH" commit "docs: initialize GSD project with roadmap and requirements" --files ".planning/"
    ```

    ## Step 6: Summary

    Report to the user:
    - Project initialized at `.planning/`
    - Key decisions from research
    - Phase count and roadmap overview
    - Next step: `/gsd:plan-phase 1` to start planning Phase 1
    ```

    **Critical rules:**
    - @-references to stage files appear ONLY at the relevant step, NOT in a header preamble
    - No AskUserQuestion calls anywhere — all interaction is via normal chat
    - All gsd-tools calls use `$GSD_TOOLS_PATH` environment variable
    - Stage @-references use relative paths (e.g., `@./stage-setup.md`)
  </action>
  <verify>
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/new-project/SKILL.md` shows `name: gsd:new-project` and `user-invocable: true`
    Verify stage references: `grep "@.*stage-" openclaw-plugin/skills/workflows/new-project/SKILL.md | wc -l` (should be 2)
    Verify no AskUserQuestion: `grep -i "AskUserQuestion" openclaw-plugin/skills/workflows/new-project/SKILL.md | wc -l` (should be 0)
    Verify GSD_TOOLS_PATH usage: `grep "GSD_TOOLS_PATH" openclaw-plugin/skills/workflows/new-project/SKILL.md`
  </verify>
  <done>Orchestrator SKILL.md created with user-invocable frontmatter, 6-step recipe, @-references to stage files at point of use only, no AskUserQuestion, and GSD_TOOLS_PATH for all tool calls.</done>
</task>

<task type="auto">
  <name>Task 2: Create stage-setup.md with researcher and synthesizer agent roles</name>
  <files>openclaw-plugin/skills/workflows/new-project/stage-setup.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/new-project/stage-setup.md` — embeds the gsd-project-researcher and gsd-research-synthesizer agent roles verbatim.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd-new-project-stage-setup
    description: Research and synthesis stage for /gsd:new-project — internal use only
    user-invocable: false
    os: ["darwin", "linux"]
    ---
    ```

    **Body structure:**

    1. **Section 1: Research Phase** — Copy the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-project-researcher.md` verbatim inside a clearly marked block:
       ```markdown
       <!-- AGENT ROLE: gsd-project-researcher -->
       <!-- Source: gsd-project-researcher.md (verbatim copy — DO NOT modify) -->

       [FULL VERBATIM COPY of gsd-project-researcher.md content here]
       ```

    2. **Section 2: Research Instructions** — After the agent role, provide step-by-step instructions for the LLM to execute the research:
       ```markdown
       ## Research Execution

       Execute 4 research passes using the researcher role above:

       1. **STACK research**: Research technology stack decisions, frameworks, and libraries relevant to the project
       2. **FEATURES research**: Research feature requirements, user stories, and acceptance criteria
       3. **ARCHITECTURE research**: Research architecture patterns, data models, and system design
       4. **PITFALLS research**: Research common pitfalls, anti-patterns, and risks for this type of project

       For each research pass:
       - Apply the gsd-project-researcher role above
       - Focus on the specific domain (STACK/FEATURES/ARCHITECTURE/PITFALLS)
       - Use available tools (WebSearch, Read, Grep) to gather information
       - Produce structured findings

       Collect all 4 research outputs for the synthesis step.
       ```

    3. **Section 3: Synthesis Phase** — Copy the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-research-synthesizer.md` verbatim:
       ```markdown
       <!-- AGENT ROLE: gsd-research-synthesizer -->
       <!-- Source: gsd-research-synthesizer.md (verbatim copy — DO NOT modify) -->

       [FULL VERBATIM COPY of gsd-research-synthesizer.md content here]
       ```

    4. **Section 4: Synthesis Instructions**:
       ```markdown
       ## Synthesis Execution

       Using the gsd-research-synthesizer role above:
       1. Take all 4 research outputs
       2. Synthesize into unified project context
       3. Write PROJECT.md using the GSD project template
       4. Write REQUIREMENTS.md with prioritized requirements
       5. Return synthesis summary for the roadmap stage
       ```

    **CRITICAL:** The agent role text must be a COMPLETE, VERBATIM copy from the source agent files. Do not summarize, truncate, or paraphrase any instructions, guardrails, or examples from the original agent definitions.
  </action>
  <verify>
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/new-project/stage-setup.md` shows `user-invocable: false`
    Verify researcher role embedded: `grep "gsd-project-researcher" openclaw-plugin/skills/workflows/new-project/stage-setup.md`
    Verify synthesizer role embedded: `grep "gsd-research-synthesizer" openclaw-plugin/skills/workflows/new-project/stage-setup.md`
    Verify no @-references to user-local agent paths: `grep "@/Users/" openclaw-plugin/skills/workflows/new-project/stage-setup.md | wc -l` (should be 0)
    Verify file is substantial (agent prompts are large): `wc -l openclaw-plugin/skills/workflows/new-project/stage-setup.md` (should be 200+ lines)
  </verify>
  <done>stage-setup.md created with gsd-project-researcher and gsd-research-synthesizer agent roles embedded verbatim. Not user-invocable. No external @-path references to user-local agent files. Research instructions cover all 4 domains (STACK, FEATURES, ARCHITECTURE, PITFALLS).</done>
</task>

<task type="auto">
  <name>Task 3: Create stage-roadmap.md with roadmapper agent role</name>
  <files>openclaw-plugin/skills/workflows/new-project/stage-roadmap.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/new-project/stage-roadmap.md` — embeds the gsd-roadmapper agent role verbatim.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd-new-project-stage-roadmap
    description: Roadmap creation stage for /gsd:new-project — internal use only
    user-invocable: false
    os: ["darwin", "linux"]
    ---
    ```

    **Body structure:**

    1. **Section 1: Roadmapper Role** — Copy the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-roadmapper.md` verbatim:
       ```markdown
       <!-- AGENT ROLE: gsd-roadmapper -->
       <!-- Source: gsd-roadmapper.md (verbatim copy — DO NOT modify) -->

       [FULL VERBATIM COPY of gsd-roadmapper.md content here]
       ```

    2. **Section 2: Roadmap Instructions**:
       ```markdown
       ## Roadmap Execution

       Using the gsd-roadmapper role above:
       1. Read the synthesized PROJECT.md and REQUIREMENTS.md from Stage 1
       2. Design phases that deliver incremental value
       3. Write ROADMAP.md with phase goals, requirements mapping, and success criteria
       4. Write STATE.md with initial project position
       5. Write .planning/config.json with default workflow settings

       Use gsd-tools for file operations:
       ```bash
       # Write config.json with defaults
       node "$GSD_TOOLS_PATH" state init
       ```

       Return: "ROADMAP COMPLETE" with phase count and overview summary.
       ```

    **CRITICAL:** The agent role text must be a COMPLETE, VERBATIM copy from the source agent file. Do not summarize, truncate, or paraphrase.
  </action>
  <verify>
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/new-project/stage-roadmap.md` shows `user-invocable: false`
    Verify roadmapper role embedded: `grep "gsd-roadmapper" openclaw-plugin/skills/workflows/new-project/stage-roadmap.md`
    Verify no @-references to user-local paths: `grep "@/Users/" openclaw-plugin/skills/workflows/new-project/stage-roadmap.md | wc -l` (should be 0)
    Verify file is substantial: `wc -l openclaw-plugin/skills/workflows/new-project/stage-roadmap.md` (should be 100+ lines)
  </verify>
  <done>stage-roadmap.md created with gsd-roadmapper agent role embedded verbatim. Not user-invocable. No external @-path references. Roadmap instructions produce ROADMAP.md, STATE.md, and config.json.</done>
</task>

</tasks>

<verification>
1. `ls openclaw-plugin/skills/workflows/new-project/` lists SKILL.md, stage-setup.md, stage-roadmap.md
2. `grep "name: gsd:new-project" openclaw-plugin/skills/workflows/new-project/SKILL.md` confirms correct name
3. `grep "user-invocable: true" openclaw-plugin/skills/workflows/new-project/SKILL.md` confirms user-invocable
4. `grep "user-invocable: false" openclaw-plugin/skills/workflows/new-project/stage-setup.md` confirms internal
5. `grep "user-invocable: false" openclaw-plugin/skills/workflows/new-project/stage-roadmap.md` confirms internal
6. `grep -r "@/Users/" openclaw-plugin/skills/workflows/new-project/` returns NO results (no user-local paths)
7. `grep -c "AskUserQuestion" openclaw-plugin/skills/workflows/new-project/*.md` returns 0 for all files
8. All 3 agent roles (gsd-project-researcher, gsd-research-synthesizer, gsd-roadmapper) appear in stage files
</verification>

<success_criteria>
- /gsd:new-project orchestrator is user-invocable with step-by-step recipe
- Stage files embed all 3 agent roles verbatim (researcher, synthesizer, roadmapper)
- No AskUserQuestion usage — fully autonomous
- No @-references to /Users/ paths — self-contained
- @-references to stage files appear only at point of use in orchestrator
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-workflows/02-02-SUMMARY.md` per the summary template.
</output>
