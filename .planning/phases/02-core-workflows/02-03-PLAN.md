---
phase: 02-core-workflows
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - openclaw-plugin/skills/workflows/plan-phase/SKILL.md
  - openclaw-plugin/skills/workflows/plan-phase/stage-research.md
  - openclaw-plugin/skills/workflows/plan-phase/stage-planning.md
  - openclaw-plugin/skills/workflows/plan-phase/stage-verification.md
autonomous: true
requirements:
  - CMD-03
  - AGNT-01
  - AGNT-02

must_haves:
  truths:
    - "User can run /gsd:plan-phase <N> and it produces PLAN.md files for the specified phase"
    - "Research stage produces a RESEARCH.md in the phase directory when research_enabled is true"
    - "Planning stage produces PLAN.md files with valid frontmatter (phase, plan, wave, depends_on, files_modified, autonomous, requirements, must_haves)"
    - "Verification stage runs gsd-plan-checker logic and triggers up to 3 revision loops if issues are found"
    - "The gsd-phase-researcher agent role is faithfully embedded in stage-research.md"
    - "The gsd-planner agent role is faithfully embedded in stage-planning.md"
    - "The gsd-plan-checker agent role is faithfully embedded in stage-verification.md"
  artifacts:
    - path: "openclaw-plugin/skills/workflows/plan-phase/SKILL.md"
      provides: "Orchestrator for /gsd:plan-phase — user-invocable, 5-step recipe with conditional research and revision loop"
      contains: "name: gsd:plan-phase"
    - path: "openclaw-plugin/skills/workflows/plan-phase/stage-research.md"
      provides: "Research stage embedding gsd-phase-researcher role"
      contains: "user-invocable: false"
    - path: "openclaw-plugin/skills/workflows/plan-phase/stage-planning.md"
      provides: "Planning stage embedding gsd-planner role"
      contains: "user-invocable: false"
    - path: "openclaw-plugin/skills/workflows/plan-phase/stage-verification.md"
      provides: "Verification stage embedding gsd-plan-checker role with revision loop"
      contains: "user-invocable: false"
  key_links:
    - from: "openclaw-plugin/skills/workflows/plan-phase/SKILL.md"
      to: "openclaw-plugin/skills/workflows/plan-phase/stage-research.md"
      via: "@-path reference at Step 2"
      pattern: "@.*stage-research\\.md"
    - from: "openclaw-plugin/skills/workflows/plan-phase/SKILL.md"
      to: "openclaw-plugin/skills/workflows/plan-phase/stage-planning.md"
      via: "@-path reference at Step 3"
      pattern: "@.*stage-planning\\.md"
    - from: "openclaw-plugin/skills/workflows/plan-phase/SKILL.md"
      to: "openclaw-plugin/skills/workflows/plan-phase/stage-verification.md"
      via: "@-path reference at Step 4"
      pattern: "@.*stage-verification\\.md"
---

<objective>
Create the full /gsd:plan-phase workflow as an orchestrator SKILL.md with 3 stage skills embedding the phase-researcher, planner, and plan-checker agent roles inline.

Purpose: This is the core planning command that takes a phase from the roadmap and produces executable PLAN.md files. It runs research (optional), planning, and verification with up to 3 revision loops — all autonomously.
Output: 4 SKILL.md files — orchestrator (user-invocable), stage-research.md, stage-planning.md, stage-verification.md.
</objective>

<execution_context>
@/Users/egorproskyrin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorproskyrin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-workflows/02-CONTEXT.md
@.planning/phases/02-core-workflows/02-RESEARCH.md
@.planning/phases/02-core-workflows/02-01-SUMMARY.md

<!-- Source agent files to port verbatim into stage skills -->
@/Users/egorproskyrin/.claude/agents/gsd-phase-researcher.md
@/Users/egorproskyrin/.claude/agents/gsd-planner.md
@/Users/egorproskyrin/.claude/agents/gsd-plan-checker.md

<!-- Source workflow to port -->
@/Users/egorproskyrin/.claude/get-shit-done/workflows/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:plan-phase orchestrator SKILL.md</name>
  <files>openclaw-plugin/skills/workflows/plan-phase/SKILL.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/plan-phase/SKILL.md` — the user-invocable orchestrator.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd:plan-phase
    description: Create executable plans for a GSD roadmap phase. Runs research, planning, and verification stages.
    user-invocable: true
    os: ["darwin", "linux"]
    ---
    ```

    **Body structure — step-by-step recipe:**

    ```markdown
    # /gsd:plan-phase

    Create executable plans for a GSD roadmap phase.

    ## Arguments

    The user provides the phase number as an argument: `/gsd:plan-phase 2` or `/gsd:plan-phase 02`.
    If no argument provided, ask the user which phase to plan (do NOT use AskUserQuestion — just ask in chat).

    ## Step 1: Initialize

    Run init to get phase context:
    ```bash
    INIT=$(node "$GSD_TOOLS_PATH" init plan-phase "$PHASE_ARG")
    ```
    Parse JSON output for: researcher_model, planner_model, checker_model, research_enabled, plan_checker_enabled, commit_docs, phase_found, phase_dir, phase_number, padded_phase, phase_req_ids, has_research, has_context, has_plans, state_path, roadmap_path, requirements_path.

    If `phase_found` is false: inform the user the phase was not found and stop.
    If `has_plans` is true: inform the user plans already exist. Ask if they want to re-plan (overwrite) or stop.

    ## Step 2: Research (conditional)

    If `research_enabled` is true AND `has_research` is false:

    Follow the research stage:
    @./stage-research.md

    Pass the phase context (phase_dir, phase_number, phase_req_ids, roadmap_path, requirements_path) to the stage.

    If `has_research` is true: skip research, inform the user research already exists.

    ## Step 3: Plan

    Follow the planning stage:
    @./stage-planning.md

    Pass full init context plus research output (if any) to the stage.

    ## Step 4: Verify Plans (conditional, with revision loop)

    If `plan_checker_enabled` is true:

    Follow the verification stage:
    @./stage-verification.md

    If verification returns ISSUES FOUND:
    - Loop back to stage-planning.md in REVISION MODE (pass checker issues as revision_context)
    - Re-verify after revision
    - Maximum 3 revision loops. After 3, accept plans as-is with warnings.

    ## Step 5: Commit

    If `commit_docs` is true:
    ```bash
    node "$GSD_TOOLS_PATH" commit "docs($(printf '%02d' $PHASE_NUMBER)): create phase plans" --files "$PHASE_DIR/"
    ```

    Report to user: plans created, wave structure, next step is `/gsd:execute-phase $PHASE_NUMBER`.
    ```

    **Critical rules:**
    - @-references to stage files at point of use ONLY
    - No AskUserQuestion
    - Revision loop capped at 3 iterations
    - All gsd-tools calls use $GSD_TOOLS_PATH
    - Stage references use relative paths
  </action>
  <verify>
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/plan-phase/SKILL.md` shows `name: gsd:plan-phase`
    Verify stage references: `grep "@.*stage-" openclaw-plugin/skills/workflows/plan-phase/SKILL.md | wc -l` (should be 3)
    Verify revision loop mentioned: `grep -i "revision" openclaw-plugin/skills/workflows/plan-phase/SKILL.md`
  </verify>
  <done>Orchestrator SKILL.md created with 5-step recipe including conditional research, planning, verification with up to 3 revision loops, and git commit.</done>
</task>

<task type="auto">
  <name>Task 2: Create stage-research.md and stage-planning.md with agent roles</name>
  <files>
    openclaw-plugin/skills/workflows/plan-phase/stage-research.md
    openclaw-plugin/skills/workflows/plan-phase/stage-planning.md
  </files>
  <action>
    **File 1: stage-research.md**

    Frontmatter:
    ```yaml
    ---
    name: gsd-plan-phase-stage-research
    description: Research stage for /gsd:plan-phase — internal use only
    user-invocable: false
    os: ["darwin", "linux"]
    ---
    ```

    Body:
    1. Embed the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-phase-researcher.md` verbatim inside a marked block:
       ```markdown
       <!-- AGENT ROLE: gsd-phase-researcher -->
       <!-- Source: gsd-phase-researcher.md (verbatim copy — DO NOT modify) -->
       [FULL VERBATIM COPY]
       ```
    2. Add research execution instructions:
       - Read ROADMAP.md, REQUIREMENTS.md, and any existing CONTEXT.md for the phase
       - Follow the gsd-phase-researcher role to research what's needed to plan this phase well
       - Write XX-RESEARCH.md to the phase directory
       - If commit_docs: commit via `node "$GSD_TOOLS_PATH" commit`
       - Return: "RESEARCH COMPLETE" with key findings summary

    **File 2: stage-planning.md**

    Frontmatter:
    ```yaml
    ---
    name: gsd-plan-phase-stage-planning
    description: Planning stage for /gsd:plan-phase — internal use only
    user-invocable: false
    os: ["darwin", "linux"]
    ---
    ```

    Body:
    1. Embed the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-planner.md` verbatim inside a marked block:
       ```markdown
       <!-- AGENT ROLE: gsd-planner -->
       <!-- Source: gsd-planner.md (verbatim copy — DO NOT modify) -->
       [FULL VERBATIM COPY]
       ```
    2. Add planning execution instructions:
       - Read phase context from orchestrator (phase_dir, phase_req_ids, research output)
       - Read ROADMAP.md, REQUIREMENTS.md, STATE.md, and RESEARCH.md
       - Follow the gsd-planner role to decompose the phase into PLAN.md files
       - Write PLAN.md files to the phase directory using the Write tool (never heredoc)
       - Return: "PLANNING COMPLETE" with plan count and wave structure

    3. Add REVISION MODE section:
       - When called with `<revision_context>`, parse checker issues
       - Follow the gsd-planner revision_mode instructions to make targeted updates
       - Do NOT rewrite plans from scratch — edit specific flagged sections
       - Return: "REVISION COMPLETE" with changes made

    **CRITICAL:** Both agent role texts must be COMPLETE, VERBATIM copies. gsd-planner.md is ~50KB — embed ALL of it. Do not truncate.
  </action>
  <verify>
    Verify researcher role: `grep "gsd-phase-researcher" openclaw-plugin/skills/workflows/plan-phase/stage-research.md`
    Verify planner role: `grep "gsd-planner" openclaw-plugin/skills/workflows/plan-phase/stage-planning.md`
    Verify revision mode: `grep -i "revision" openclaw-plugin/skills/workflows/plan-phase/stage-planning.md`
    Verify no user-local paths: `grep -r "@/Users/" openclaw-plugin/skills/workflows/plan-phase/stage-research.md openclaw-plugin/skills/workflows/plan-phase/stage-planning.md | wc -l` (should be 0)
    Verify planning stage is large (gsd-planner is ~50KB): `wc -l openclaw-plugin/skills/workflows/plan-phase/stage-planning.md` (should be 1000+ lines)
  </verify>
  <done>stage-research.md and stage-planning.md created with full verbatim agent roles. Planning stage includes revision mode support. No external @-path references.</done>
</task>

<task type="auto">
  <name>Task 3: Create stage-verification.md with plan-checker agent role</name>
  <files>openclaw-plugin/skills/workflows/plan-phase/stage-verification.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/plan-phase/stage-verification.md`.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd-plan-phase-stage-verification
    description: Plan verification stage for /gsd:plan-phase — internal use only
    user-invocable: false
    os: ["darwin", "linux"]
    ---
    ```

    **Body:**
    1. Embed the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-plan-checker.md` verbatim:
       ```markdown
       <!-- AGENT ROLE: gsd-plan-checker -->
       <!-- Source: gsd-plan-checker.md (verbatim copy — DO NOT modify) -->
       [FULL VERBATIM COPY]
       ```
    2. Add verification execution instructions:
       - Read all PLAN.md files from the phase directory
       - Follow the gsd-plan-checker role to verify plans against all quality dimensions
       - Use `node "$GSD_TOOLS_PATH" verify plan-structure <plan_path>` for structural validation
       - If all checks pass: return "VERIFICATION PASSED" with summary
       - If issues found: return "ISSUES FOUND" with structured issue list formatted as `<revision_context>` for the planning stage to consume
       - The orchestrator handles the revision loop — this stage just reports results

    **CRITICAL:** The plan-checker agent role must be a COMPLETE, VERBATIM copy.
  </action>
  <verify>
    Verify checker role: `grep "gsd-plan-checker" openclaw-plugin/skills/workflows/plan-phase/stage-verification.md`
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/plan-phase/stage-verification.md` shows `user-invocable: false`
    Verify no user-local paths: `grep "@/Users/" openclaw-plugin/skills/workflows/plan-phase/stage-verification.md | wc -l` (should be 0)
    Verify verification output format: `grep "VERIFICATION PASSED\|ISSUES FOUND" openclaw-plugin/skills/workflows/plan-phase/stage-verification.md`
  </verify>
  <done>stage-verification.md created with gsd-plan-checker agent role embedded verbatim. Returns "VERIFICATION PASSED" or "ISSUES FOUND" with structured revision_context for the orchestrator's revision loop.</done>
</task>

</tasks>

<verification>
1. `ls openclaw-plugin/skills/workflows/plan-phase/` lists SKILL.md, stage-research.md, stage-planning.md, stage-verification.md
2. `grep "name: gsd:plan-phase" openclaw-plugin/skills/workflows/plan-phase/SKILL.md` confirms correct name
3. `grep "user-invocable: true" openclaw-plugin/skills/workflows/plan-phase/SKILL.md` confirms user-invocable
4. `grep "user-invocable: false" openclaw-plugin/skills/workflows/plan-phase/stage-*.md | wc -l` returns 3
5. `grep -r "@/Users/" openclaw-plugin/skills/workflows/plan-phase/` returns NO results
6. All 3 agent roles (gsd-phase-researcher, gsd-planner, gsd-plan-checker) appear in stage files
7. Revision loop logic present in orchestrator and stage-planning.md
</verification>

<success_criteria>
- /gsd:plan-phase orchestrator has conditional research, planning, and verification stages
- Revision loop capped at 3 iterations between planning and verification
- All 3 agent roles embedded verbatim in stage files
- No AskUserQuestion, no @-references to /Users/ paths
- Stage files are not user-invocable
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-workflows/02-03-SUMMARY.md` per the summary template.
</output>
