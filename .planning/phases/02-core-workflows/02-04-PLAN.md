---
phase: 02-core-workflows
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - openclaw-plugin/skills/workflows/execute-phase/SKILL.md
  - openclaw-plugin/skills/workflows/execute-phase/stage-executor.md
autonomous: true
requirements:
  - CMD-03
  - ORCH-02
  - ORCH-03

must_haves:
  truths:
    - "User can run /gsd:execute-phase <N> and it executes all plans in the phase sequentially by wave"
    - "wave-state.json is created in the phase directory before execution begins with all plans listed as pending"
    - "wave-state.json is updated after each plan completes, marking it as complete with a timestamp"
    - "Interrupted execution resumes from the first incomplete plan by reading wave-state.json"
    - "Each completed plan gets an atomic git commit via gsd-tools commit"
    - "The gsd-executor agent role is faithfully embedded in stage-executor.md"
  artifacts:
    - path: "openclaw-plugin/skills/workflows/execute-phase/SKILL.md"
      provides: "Orchestrator for /gsd:execute-phase — user-invocable, wave loop with state tracking"
      contains: "name: gsd:execute-phase"
    - path: "openclaw-plugin/skills/workflows/execute-phase/stage-executor.md"
      provides: "Executor stage embedding gsd-executor role"
      contains: "user-invocable: false"
  key_links:
    - from: "openclaw-plugin/skills/workflows/execute-phase/SKILL.md"
      to: "openclaw-plugin/skills/workflows/execute-phase/stage-executor.md"
      via: "@-path reference inside the wave execution loop"
      pattern: "@.*stage-executor\\.md"
    - from: "openclaw-plugin/skills/workflows/execute-phase/SKILL.md"
      to: "wave-state.json"
      via: "Write tool creates/updates wave-state.json in phase directory"
      pattern: "wave-state\\.json"
    - from: "openclaw-plugin/skills/workflows/execute-phase/SKILL.md"
      to: "gsd-tools.cjs"
      via: "phase-plan-index subcommand for wave grouping and commit subcommand for atomic commits"
      pattern: "phase-plan-index|gsd-tools.*commit"
---

<objective>
Create the full /gsd:execute-phase workflow with wave-based sequential execution, wave-state.json state tracking, and resume-from-interruption capability.

Purpose: This is the command that turns plans into code. It reads PLAN.md files, groups them by wave, executes each plan using the gsd-executor agent role, commits after each plan, and tracks progress in wave-state.json for safe resume.
Output: 2 SKILL.md files — orchestrator (user-invocable, wave loop), stage-executor.md (executor agent role).
</objective>

<execution_context>
@/Users/egorproskyrin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorproskyrin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-workflows/02-CONTEXT.md
@.planning/phases/02-core-workflows/02-RESEARCH.md
@.planning/phases/02-core-workflows/02-01-SUMMARY.md

<!-- Source agent file to port verbatim -->
@/Users/egorproskyrin/.claude/agents/gsd-executor.md

<!-- Source workflow to port -->
@/Users/egorproskyrin/.claude/get-shit-done/workflows/execute-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:execute-phase orchestrator SKILL.md with wave loop</name>
  <files>openclaw-plugin/skills/workflows/execute-phase/SKILL.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/execute-phase/SKILL.md` — the user-invocable orchestrator with wave execution loop and state tracking.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd:execute-phase
    description: Execute all plans in a GSD roadmap phase. Runs plans sequentially by wave with state tracking and atomic commits.
    user-invocable: true
    os: ["darwin", "linux"]
    ---
    ```

    **Body structure — step-by-step recipe with wave loop:**

    ```markdown
    # /gsd:execute-phase

    Execute all plans in a GSD roadmap phase.

    ## Arguments

    The user provides the phase number: `/gsd:execute-phase 2` or `/gsd:execute-phase 02`.
    If no argument provided, ask which phase to execute.

    ## Step 1: Initialize

    Run init to get phase context:
    ```bash
    INIT=$(node "$GSD_TOOLS_PATH" init execute-phase "$PHASE_ARG")
    ```
    Parse JSON: executor_model, commit_docs, phase_found, phase_dir, phase_number, plans, incomplete_plans, plan_count, incomplete_count.

    If `phase_found` is false: inform user and stop.
    If `plan_count` is 0: inform user no plans exist, suggest running `/gsd:plan-phase` first.

    ## Step 2: Get Wave Structure

    ```bash
    WAVE_INDEX=$(node "$GSD_TOOLS_PATH" phase-plan-index "$PHASE_ARG")
    ```
    Parse JSON: plans[], waves{}, incomplete[], has_checkpoints.

    ## Step 3: Initialize or Resume wave-state.json

    Check if `$PHASE_DIR/wave-state.json` already exists.

    **If it exists (RESUME mode):**
    Read the existing wave-state.json. Identify plans with status !== "complete".
    Inform user: "Resuming execution from wave N, plan XX-NN".

    **If it does NOT exist (FRESH mode):**
    Create wave-state.json with all plans as pending:
    ```json
    {
      "phase": "<phase_number>",
      "phase_dir": "<phase_dir>",
      "created": "<ISO timestamp>",
      "updated": "<ISO timestamp>",
      "waves": { <from WAVE_INDEX.waves> },
      "plans": {
        "<plan_id>": { "status": "pending", "completed_at": null }
      },
      "current_wave": 1,
      "total_plans": <count>,
      "completed_plans": 0
    }
    ```
    Write this to `$PHASE_DIR/wave-state.json` using the Write tool.

    ## Step 4: Execute Plans by Wave

    For each wave (1, 2, 3, ...) in order:
      For each plan_id in the wave:
        - Skip if wave-state.json shows `status: "complete"` for this plan
        - Read the PLAN.md file for this plan from the phase directory
        - Follow the executor stage to execute this plan:
          @./stage-executor.md
        - After successful execution:
          1. Commit via: `node "$GSD_TOOLS_PATH" commit "<plan summary message>" --files "<modified files>"`
          2. Create SUMMARY.md for the plan: `$PHASE_DIR/<phase>-<plan>-SUMMARY.md`
          3. Update wave-state.json: set plan status to "complete", update completed_at timestamp, increment completed_plans, update "updated" timestamp
          4. Write updated wave-state.json using the Write tool

    **IMPORTANT:** Update wave-state.json AFTER each plan completes, not at the end of all plans. This ensures resume works correctly if interrupted.

    ## Step 5: Update State

    After all plans complete:
    ```bash
    node "$GSD_TOOLS_PATH" state advance-plan "$PHASE_NUMBER"
    ```

    ## Step 6: Report

    Report to user:
    - Plans executed: N/N
    - Wave structure summary
    - Next step: `/gsd:verify-work $PHASE_NUMBER` to verify the phase
    ```

    **Critical rules:**
    - wave-state.json written BEFORE execution starts (all pending), updated AFTER each plan
    - Plans in the same wave still execute sequentially (v1 — no parallel execution)
    - Skip completed plans on resume — never re-execute a completed plan
    - @-reference to stage-executor.md inside the loop, not in header
    - Atomic git commits after each plan, not after all plans
  </action>
  <verify>
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/execute-phase/SKILL.md` shows `name: gsd:execute-phase`
    Verify wave-state.json handling: `grep "wave-state.json" openclaw-plugin/skills/workflows/execute-phase/SKILL.md | wc -l` (should be >= 5)
    Verify resume logic: `grep -i "resume" openclaw-plugin/skills/workflows/execute-phase/SKILL.md`
    Verify stage reference: `grep "@.*stage-executor" openclaw-plugin/skills/workflows/execute-phase/SKILL.md`
    Verify atomic commits: `grep "gsd-tools.*commit\|GSD_TOOLS_PATH.*commit" openclaw-plugin/skills/workflows/execute-phase/SKILL.md`
  </verify>
  <done>Orchestrator SKILL.md created with wave loop, wave-state.json initialization/resume, per-plan atomic commits, and state advancement after completion.</done>
</task>

<task type="auto">
  <name>Task 2: Create stage-executor.md with gsd-executor agent role</name>
  <files>openclaw-plugin/skills/workflows/execute-phase/stage-executor.md</files>
  <action>
    Create `openclaw-plugin/skills/workflows/execute-phase/stage-executor.md` — embeds the gsd-executor agent role verbatim.

    **Frontmatter:**
    ```yaml
    ---
    name: gsd-execute-phase-stage-executor
    description: Plan execution stage for /gsd:execute-phase — internal use only
    user-invocable: false
    os: ["darwin", "linux"]
    ---
    ```

    **Body:**
    1. Embed the ENTIRE content of `/Users/egorproskyrin/.claude/agents/gsd-executor.md` verbatim:
       ```markdown
       <!-- AGENT ROLE: gsd-executor -->
       <!-- Source: gsd-executor.md (verbatim copy — DO NOT modify) -->

       [FULL VERBATIM COPY of gsd-executor.md content here]
       ```

    2. Add execution instructions:
       ```markdown
       ## Execution Instructions

       You are executing a single PLAN.md. The orchestrator has passed you:
       - The plan content (read from PLAN.md file)
       - The phase context (phase_dir, phase_number)

       Follow the gsd-executor role above to:
       1. Parse the plan's <objective>, <context>, and <tasks>
       2. Execute each <task> in order
       3. For each task:
          - Read the <action> and implement it
          - Run the <verify> checks
          - Confirm the <done> criteria is met
       4. After all tasks complete, return execution summary

       **Rules:**
       - Use the Write tool for file creation (never heredoc/cat)
       - Use Bash for shell commands, test runners, verifications
       - If a task's verify check fails, fix the issue before moving to the next task
       - Do NOT skip tasks or mark them done if verify fails
       ```

    **CRITICAL:** The gsd-executor agent role must be a COMPLETE, VERBATIM copy.
  </action>
  <verify>
    Verify executor role: `grep "gsd-executor" openclaw-plugin/skills/workflows/execute-phase/stage-executor.md`
    Verify frontmatter: `head -5 openclaw-plugin/skills/workflows/execute-phase/stage-executor.md` shows `user-invocable: false`
    Verify no user-local paths: `grep "@/Users/" openclaw-plugin/skills/workflows/execute-phase/stage-executor.md | wc -l` (should be 0)
  </verify>
  <done>stage-executor.md created with gsd-executor agent role embedded verbatim. Execution instructions cover task parsing, implementation, verification, and error handling.</done>
</task>

</tasks>

<verification>
1. `ls openclaw-plugin/skills/workflows/execute-phase/` lists SKILL.md, stage-executor.md
2. `grep "name: gsd:execute-phase" openclaw-plugin/skills/workflows/execute-phase/SKILL.md`
3. `grep "user-invocable: true" openclaw-plugin/skills/workflows/execute-phase/SKILL.md`
4. `grep "user-invocable: false" openclaw-plugin/skills/workflows/execute-phase/stage-executor.md`
5. `grep -r "@/Users/" openclaw-plugin/skills/workflows/execute-phase/` returns NO results
6. wave-state.json schema documented in orchestrator with pending/complete/failed status values
7. Resume logic clearly documented (skip completed plans)
8. Atomic commit per plan (not batch commit)
</verification>

<success_criteria>
- /gsd:execute-phase orchestrator implements full wave loop with sequential execution
- wave-state.json created before execution, updated after each plan
- Resume from interruption works by reading wave-state.json and skipping completed plans
- Atomic git commits after each plan completion
- gsd-executor agent role embedded verbatim in stage-executor.md
- No AskUserQuestion, no @-references to /Users/ paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-workflows/02-04-SUMMARY.md` per the summary template.
</output>
