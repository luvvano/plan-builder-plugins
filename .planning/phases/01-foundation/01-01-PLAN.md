---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openclaw-plugin/openclaw.plugin.json
  - openclaw-plugin/package.json
  - openclaw-plugin/tsconfig.json
  - openclaw-plugin/src/index.ts
  - openclaw-plugin/bin/gsd-tools.cjs
  - openclaw-plugin/bin/lib/state.cjs
  - openclaw-plugin/bin/lib/commands.cjs
  - openclaw-plugin/bin/lib/template.cjs
  - openclaw-plugin/bin/lib/phase.cjs
  - openclaw-plugin/bin/lib/verify.cjs
  - openclaw-plugin/bin/lib/core.cjs
  - openclaw-plugin/bin/lib/roadmap.cjs
  - openclaw-plugin/bin/lib/init.cjs
  - openclaw-plugin/bin/lib/frontmatter.cjs
  - openclaw-plugin/bin/lib/milestone.cjs
  - openclaw-plugin/bin/lib/config.cjs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Plugin loads without errors when copied to ~/.openclaw/extensions/gsd-for-openclaw/"
    - "node bin/gsd-tools.cjs resolves correctly from within the plugin directory"
    - "GSD_TOOLS_PATH environment variable is set after plugin service starts"
    - "openclaw.plugin.json passes JSON Schema validation"
  artifacts:
    - path: "openclaw-plugin/openclaw.plugin.json"
      provides: "Plugin manifest with id, name, configSchema, skills array"
      contains: "gsd-for-openclaw"
    - path: "openclaw-plugin/package.json"
      provides: "Package manifest with openclaw peerDependency"
      contains: "peerDependencies"
    - path: "openclaw-plugin/src/index.ts"
      provides: "Plugin entry point with registerService for GSD_TOOLS_PATH"
      exports: ["default"]
    - path: "openclaw-plugin/bin/gsd-tools.cjs"
      provides: "Bundled GSD tools CLI"
  key_links:
    - from: "openclaw-plugin/src/index.ts"
      to: "openclaw-plugin/bin/gsd-tools.cjs"
      via: "import.meta.dirname path resolution in registerService start()"
      pattern: "import\\.meta\\.dirname.*bin.*gsd-tools"
    - from: "openclaw-plugin/openclaw.plugin.json"
      to: "openclaw-plugin/src/index.ts"
      via: "openclaw.extensions in package.json"
      pattern: "extensions.*index\\.ts"
---

<objective>
Scaffold the OpenClaw plugin with manifest, package.json, entry point, and bundled gsd-tools.cjs with portable path resolution.

Purpose: Create the complete plugin skeleton that loads in OpenClaw without errors and sets up the GSD_TOOLS_PATH environment variable for all downstream SKILL.md files to use.
Output: openclaw-plugin/ directory with valid manifest, package.json (openclaw as peerDependency), TypeScript entry point with registerService lifecycle, and bundled gsd-tools.cjs with all lib/ dependencies.
</objective>

<execution_context>
@/Users/egorproskyrin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/egorproskyrin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@openclaw-plugin/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin manifest, package.json, and tsconfig</name>
  <files>
    openclaw-plugin/openclaw.plugin.json
    openclaw-plugin/package.json
    openclaw-plugin/tsconfig.json
  </files>
  <action>
    Replace the existing plugin files with the GSD-for-OpenClaw configuration:

    1. **openclaw.plugin.json** — Create the plugin manifest:
       ```json
       {
         "id": "gsd-for-openclaw",
         "name": "GSD for OpenClaw",
         "description": "Spec-driven development: plan, execute, and verify coding workflows",
         "version": "1.0.0",
         "skills": ["./skills"],
         "configSchema": {
           "type": "object",
           "additionalProperties": false,
           "properties": {
             "planningDir": {
               "type": "string",
               "description": "Directory for GSD planning files (default: .planning)"
             }
           }
         }
       }
       ```

    2. **package.json** — Create with openclaw as peerDependency (never workspace:*):
       ```json
       {
         "name": "gsd-for-openclaw",
         "version": "1.0.0",
         "type": "module",
         "description": "GSD spec-driven development plugin for OpenClaw",
         "main": "src/index.ts",
         "openclaw": {
           "extensions": ["./src/index.ts"]
         },
         "peerDependencies": {
           "openclaw": ">=2026.2.3-1"
         },
         "devDependencies": {
           "@sinclair/typebox": "^0.32.0",
           "typescript": "^5.3.0"
         },
         "scripts": {
           "typecheck": "tsc --noEmit"
         }
       }
       ```

    3. **tsconfig.json** — ES2022, ESNext, strict, noEmit:
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "moduleResolution": "bundler",
           "strict": true,
           "skipLibCheck": true,
           "noEmit": true
         },
         "include": ["src/**/*"]
       }
       ```
  </action>
  <verify>
    Verify JSON validity: `node -e "JSON.parse(require('fs').readFileSync('openclaw-plugin/openclaw.plugin.json','utf8'))" && echo "manifest OK"`
    Verify package.json has peerDependency: `node -e "const p=JSON.parse(require('fs').readFileSync('openclaw-plugin/package.json','utf8')); if(!p.peerDependencies?.openclaw) throw new Error('missing peerDep'); console.log('peerDep OK:', p.peerDependencies.openclaw)"`
  </verify>
  <done>All three config files exist with correct structure. openclaw.plugin.json has id "gsd-for-openclaw" and skills array. package.json has openclaw peerDependency. tsconfig targets ES2022 with noEmit.</done>
</task>

<task type="auto">
  <name>Task 2: Bundle gsd-tools.cjs and create plugin entry point</name>
  <files>
    openclaw-plugin/bin/gsd-tools.cjs
    openclaw-plugin/bin/lib/state.cjs
    openclaw-plugin/bin/lib/commands.cjs
    openclaw-plugin/bin/lib/template.cjs
    openclaw-plugin/bin/lib/phase.cjs
    openclaw-plugin/bin/lib/verify.cjs
    openclaw-plugin/bin/lib/core.cjs
    openclaw-plugin/bin/lib/roadmap.cjs
    openclaw-plugin/bin/lib/init.cjs
    openclaw-plugin/bin/lib/frontmatter.cjs
    openclaw-plugin/bin/lib/milestone.cjs
    openclaw-plugin/bin/lib/config.cjs
    openclaw-plugin/src/index.ts
  </files>
  <action>
    1. **Copy gsd-tools.cjs and lib/ directory** from `~/.claude/get-shit-done/bin/` into `openclaw-plugin/bin/`:
       ```bash
       mkdir -p openclaw-plugin/bin/lib
       cp ~/.claude/get-shit-done/bin/gsd-tools.cjs openclaw-plugin/bin/
       cp ~/.claude/get-shit-done/bin/lib/*.cjs openclaw-plugin/bin/lib/
       ```

    2. **Replace openclaw-plugin/src/index.ts** with the GSD plugin entry point:
       ```typescript
       import { join } from "node:path";
       import type { PluginContext } from "openclaw/plugin-sdk/core";

       export default function gsdPlugin(api: PluginContext): void {
         const cfg = api.config?.plugins?.entries?.["gsd-for-openclaw"]?.config ?? {};
         const planningDir: string = (cfg as Record<string, unknown>).planningDir as string ?? ".planning";

         api.logger.info("[gsd] plugin loaded, planningDir=" + planningDir);

         // Lifecycle service: set GSD_TOOLS_PATH and GSD_HOME for all SKILL.md files
         api.registerService({
           id: "gsd-lifecycle",
           start() {
             const toolsPath = join(import.meta.dirname, "..", "bin", "gsd-tools.cjs");
             const gsdHome = join(import.meta.dirname, "..");
             process.env.GSD_TOOLS_PATH = toolsPath;
             process.env.GSD_HOME = gsdHome;
             api.logger.info("[gsd] GSD_TOOLS_PATH=" + toolsPath);
             api.logger.info("[gsd] GSD_HOME=" + gsdHome);
           },
           stop() {
             delete process.env.GSD_TOOLS_PATH;
             delete process.env.GSD_HOME;
             api.logger.info("[gsd] cleanup complete");
           },
         });
       }
       ```

       Note: Do NOT add registerCommand or registerTool calls yet — those are validated in Plan 02.
  </action>
  <verify>
    Verify gsd-tools.cjs runs: `node openclaw-plugin/bin/gsd-tools.cjs --help 2>&1 | head -5`
    Verify lib/ files exist: `ls openclaw-plugin/bin/lib/*.cjs | wc -l` (should be 11)
    Verify index.ts has registerService: `grep "registerService" openclaw-plugin/src/index.ts`
    Verify import.meta.dirname usage: `grep "import.meta.dirname" openclaw-plugin/src/index.ts`
    Verify GSD_TOOLS_PATH is set in start(): `grep "GSD_TOOLS_PATH" openclaw-plugin/src/index.ts`
  </verify>
  <done>gsd-tools.cjs and all 11 lib/ modules are bundled in openclaw-plugin/bin/. Plugin entry point exports a default function that registers a "gsd-lifecycle" service setting GSD_TOOLS_PATH and GSD_HOME via import.meta.dirname. No hardcoded absolute paths.</done>
</task>

<task type="auto">
  <name>Task 3: Bundle GSD support files (workflows, references, templates, agents)</name>
  <files>
    openclaw-plugin/workflows/
    openclaw-plugin/references/
    openclaw-plugin/templates/
    openclaw-plugin/agents/
  </files>
  <action>
    Copy the full GSD support directory structure into the plugin so SKILL.md files can reference them via $GSD_HOME:

    ```bash
    # Copy workflows
    cp -r ~/.claude/get-shit-done/workflows openclaw-plugin/workflows

    # Copy references
    cp -r ~/.claude/get-shit-done/references openclaw-plugin/references

    # Copy templates
    cp -r ~/.claude/get-shit-done/templates openclaw-plugin/templates

    # Copy agent definitions
    cp -r ~/.claude/agents openclaw-plugin/agents
    ```

    These files are referenced by SKILL.md instructions using `$GSD_HOME/workflows/`, `$GSD_HOME/references/`, etc. instead of hardcoded `~/.claude/` paths.
  </action>
  <verify>
    Verify key directories exist:
    `ls openclaw-plugin/workflows/ | wc -l` (should be 20+)
    `ls openclaw-plugin/references/ | wc -l` (should be 13)
    `ls openclaw-plugin/templates/ | wc -l` (should be 20+)
    `ls openclaw-plugin/agents/ | wc -l` (should be 10+)
  </verify>
  <done>All GSD support files (workflows, references, templates, agents) are bundled in the plugin directory. SKILL.md files can reference them via $GSD_HOME relative paths.</done>
</task>

</tasks>

<verification>
1. `node -e "JSON.parse(require('fs').readFileSync('openclaw-plugin/openclaw.plugin.json','utf8'))"` exits 0
2. `node -e "const p=JSON.parse(require('fs').readFileSync('openclaw-plugin/package.json','utf8')); console.log(p.peerDependencies.openclaw)"` prints ">=2026.2.3-1"
3. `node openclaw-plugin/bin/gsd-tools.cjs --help` runs without MODULE_NOT_FOUND errors
4. `grep "GSD_TOOLS_PATH" openclaw-plugin/src/index.ts` finds the env var setup
5. `grep "import.meta.dirname" openclaw-plugin/src/index.ts` confirms portable path resolution
6. `ls openclaw-plugin/bin/lib/*.cjs | wc -l` returns 11
7. All support directories (workflows/, references/, templates/, agents/) are populated
</verification>

<success_criteria>
- Plugin manifest, package.json, and tsconfig are valid and correctly configured
- gsd-tools.cjs and all lib/ modules are bundled in bin/
- Entry point registers a service that sets GSD_TOOLS_PATH via import.meta.dirname
- No hardcoded absolute paths in any file
- All GSD support files are bundled alongside the plugin
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` per the summary template.
</output>
